name: CI

on:
  push:
  pull_request:

jobs:
  build:
    strategy:
      matrix:
        os: [windows-latest, macos-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - name: Placeholder
        run: echo "Build/test steps land once the CLI tech stack (docs/decisions/0001-cli-tech-stack.md) is implemented, per docs/plans/agentenv-dev-plan.md §7/§9."
