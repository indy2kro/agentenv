# cmd/agentenv

CLI entrypoint: the `agentenv` binary's `setup`, `configure`, `apply`, and
`status` commands (see `docs/plans/agentenv-dev-plan.md` §6).

Populated starting in Phase 1/3 once the CLI tech stack is decided
(`docs/decisions/0001-cli-tech-stack.md`, Phase 0 Task 6).
