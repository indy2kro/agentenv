# internal/shell

Tier 0: detect and configure a POSIX-capable shell (Git Bash) as the shell an
agent actually executes commands through, on Windows (see
`docs/plans/agentenv-dev-plan.md` §3 Tier 0, §5).

Populated starting in Phase 1, using the findings in
`docs/research/tier0-shell-fix.md` (Phase 0 Task 5).
