# internal/toolchain

`mise.toml` generation and mise/rtk invocation (see
`docs/plans/agentenv-dev-plan.md` §5, §7).

Populated starting in Phase 1, using the findings in
`docs/research/tier-tools-windows.md` and
`docs/research/tier-tools-crossplatform.md` (Phase 0 Tasks 1-2).
