# internal/adapters/opencode

Writes the OpenCode plugin script (`~/.config/opencode/plugins/`) and config
(`opencode.json`/`opencode.jsonc`) (see
`docs/plans/agentenv-dev-plan.md` §4, §5).

Populated starting in Phase 2, using the findings in
`docs/research/agent-adapters.md` and `docs/research/rtk-init-behavior.md`
(Phase 0 Tasks 3-4).
