# internal/generate

`AGENTS.md` / `CLAUDE.md` marker-block writer — regenerates only the managed
sections, leaving hand-added content untouched (see
`docs/plans/agentenv-dev-plan.md` §5, §6.5).

Populated starting in Phase 1.
